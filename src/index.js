import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>

    <a className='back' href="https://gkapps.netlify.app/">Back to projects</a>
    
    <App />
    
  </React.StrictMode>
);
